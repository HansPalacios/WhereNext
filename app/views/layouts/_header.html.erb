<header>
 			<div class="container">
 				<div class="row">
	 				<div class="col-md-6">
	 					<h1>Where Next</h1>
	 				</div>
 				 	<div class="col-md-2"></div>
          <div class="col-md-3">
          	<div id="session_links">
							<% if user_signed_in? %>
     						<%= link_to "Sign Out", destroy_user_session_path, method: :delete, :id => 'button' %>
     					<% else %>
		          	<%= link_to 'Sign Up', '#', :id => 'signup' %>
				 				<%= link_to 'Sign In', '#', :id => 'signin' %>
			 				<% end %>
			 			</div>
			 			<div class="col-md-1"></div>
          </div>
 				</div>		
 			</div>
 		</header>